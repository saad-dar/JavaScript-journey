// a statement that produces a single value other than undefined can be referred to as an expression

console.log(1 + 1) // expression

let f = function() {return 1}   // statement
f() // 1 -> function expression